package dads

// ParseMBoxMsg - parse a raw MBox message into object to be inserte dinto raw ES
func ParseMBoxMsg(msg []byte) (item map[string]interface{}, valid bool, err error) {
	// FIXME: continue
	valid = true
	return
}
